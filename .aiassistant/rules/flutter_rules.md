---
apply: always
---

Dart and Flutter coding standards live in `.agents/skills/flutter-dart/`.
Read `SKILL.md` and the topic files its router lists before editing `lib/` or `test/`.

Workflow rules live in `AGENTS.md` / `.aiassistant/rules/general_rules.md` — keep them in sync.
